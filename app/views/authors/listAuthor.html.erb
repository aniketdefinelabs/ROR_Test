<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Authors List</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f7f9fc;
      padding: 40px;
      color: #333;
    }

    h1 {
      margin-bottom: 20px;
      color: #2c3e50;
    }

    .add-author-button {
      display: inline-block;
      margin-bottom: 20px;
      background-color: #27ae60;
      color: white;
      padding: 10px 15px;
      text-decoration: none;
      border-radius: 5px;
    }

   

    table {
      width: 100%;
      border-collapse: collapse;
      background-color: #fff;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
      border-radius: 8px;
      overflow: hidden;
    }

    th, td {
      padding: 12px 16px;
      border-bottom: 1px solid #e0e0e0;
      text-align: left;
    }

    th {
      background-color: #ecf0f1;
      font-weight: 600;
    }

    ul {
      padding-left: 20px;
      margin: 0;
    }

    .action-buttons {
      white-space: nowrap;
    }

    .action-buttons a,
    .action-buttons form {
      display: inline-block;
      margin-right: 6px;
    }

    .action-buttons a {
      background-color: #3498db;
      color: white;
      padding: 6px 10px;
      text-decoration: none;
      border-radius: 4px;
    }

   

    .action-buttons input[type="submit"] {
      background-color: #e74c3c;
      color: white;
      border: none;
      padding: 6px 10px;
      border-radius: 4px;
      cursor: pointer;
    }

    .action-buttons input[type="submit"]:hover {
      background-color: #c0392b;
    }
  </style>
</head>
<body>
  <h1>Authors List</h1>

  <%= link_to "Add Author", "/authors/new", class: "add-author-button" %>

   
    <%= link_to "Add Book", "/books/newBook", class: "add-author-button" %>
  

  <table>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Date Of Birth</th>
      <th>Books</th>
      <th>Actions</th>
    </tr>
    <% @authors.each do |author| %>
      <tr>
        <td><%= author.first_name %></td>
        <td><%= author.last_name %></td>
        <td><%= author.date_of_birth %></td>
        <td>
          <% if author.books.any? %>
            <ul>
              <% author.books.each do |book| %>
                <li><%= book.book_title %></li>
              <% end %>
            </ul>
          <% else %>
            No books
          <% end %>
        </td>
        <td class="action-buttons">
          <%= link_to "Edit", "/authors/editAuthor/#{author.id}" %>
          <%= button_to "Delete", "/authors/deleteAuthor/#{author.id}", method: :delete, data: { confirm: "Are you sure?" } %>
        </td>
      </tr>
    <% end %>
  </table>
</body>
</html>
