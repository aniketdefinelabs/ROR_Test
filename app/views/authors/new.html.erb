<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Author</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f7f9fc;
      padding: 40px;
      color: #333;
    }

    h1 {
      margin-bottom: 25px;
      color: #2c3e50;
    }

    .form-container {
      background-color: white;
      padding: 25px;
      max-width: 500px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .form-container div {
      margin-bottom: 20px;
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
    }

    input[type="text"],
    input[type="date"] {
      padding: 10px;
      width: 100%;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 14px;
    }

    input[type="submit"] {
      padding: 10px 20px;
      background-color: #3498db;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 14px;
      cursor: pointer;
    }

    

    .add-author-button {
      display: inline-block;
      margin-bottom: 25px;
      background-color: #27ae60;
      color: white;
      padding: 10px 15px;
      text-decoration: none;
      border-radius: 5px;
    }

  
  </style>
</head>
<body>

  <h1>Add Author</h1>

  <%= link_to "Add Book", "/books/newBook", class: "add-author-button" %>

  <div class="form-container">
    <%= form_with scope: :author, url: "/authors/create", local: true, method: :post, data: { turbo: true } do |f| %>
      <div>
        <%= f.label :first_name, "First Name" %>
        <%= f.text_field :first_name %>
      </div>

      <div>
        <%= f.label :last_name, "Last Name" %>
        <%= f.text_field :last_name %>
      </div>

      <div>
        <%= f.label :date_of_birth, "Date of Birth" %>
        <%= f.date_field :date_of_birth %>
      </div>

      <%= f.submit "Submit", disable_with: 'Submitting...' %>
    <% end %>
  </div>

</body>
</html>
